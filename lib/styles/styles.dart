import 'package:flutter/material.dart';

class AppColors {
  static const primaryBlue = Color(0xFF08A6E4);
  static const cancelRed = Color(0xFFFA3B37);
  static const border = Color(0xFFBCC6D0);
  static const placeholder = Color(0xFF9AA7B8);
  static const subtitle = Color(0xFF888A8C);
  static const linkOrange = Color(0xFFEB5C26);
}

// Alias for backward compatibility
class CouleursApp {
  static const sousTitre = AppColors.subtitle;
  static const primaryBlue = AppColors.primaryBlue;
  static const cancelRed = AppColors.cancelRed;
  static const border = AppColors.border;
  static const placeholder = AppColors.placeholder;
  static const subtitle = AppColors.subtitle;
  static const linkOrange = AppColors.linkOrange;
}
